@page "/prompts/{promptId:int}/variable-collections/download-template"
@model DownloadTemplateModel
@{
    // This page is used for CSV template downloads
    // The actual file download is handled by the code-behind OnGet method
}
